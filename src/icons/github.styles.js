import css from "styled-jsx/css";

export default css.resolve`
    .icon {
        // fill: var(--colorText);
        width: 1.5rem;
        height: 1.5rem;
        display: block;
        vertical-align: middle;
    }
`